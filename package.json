{
  "name": "weather-app",
  "version": "1.0.0",
  "description": "This is a simple weather app that uses the OpenWeatherMap API to display current weather information for different cities. The app is built using HTML, Tailwind CSS for styling, and vanilla JavaScript.",
  "main": "tailwind.config.js",
  "scripts": {
    "build:css": "tailwindcss -i style.css -o index.css -c tailwind.config.js",
    "build:css:watch": "tailwindcss -i style.css -o index.css -c tailwind.config.js -w",
    "build:jsx": "node esbuild.js",
    "build": "tailwindcss -i style.css -o index.css -c tailwind.config.js && node esbuild.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "file:../../../Users/SHARON GODSWILL EDWI/AppData/Roaming/npm/node_modules/esbuild"
  }
}
